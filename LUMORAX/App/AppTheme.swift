import SwiftUI

enum AppTheme {
    static let gold = Color(red: 0.83, green: 0.66, blue: 0.29)
    static let softGold = Color(red: 0.95, green: 0.82, blue: 0.52)
    static let panel = Color.white.opacity(0.10)
    static let muted = Color.white.opacity(0.62)
}
